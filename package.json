{
  "name": "radlogichq",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.1",
    "vite": "^8.0.1",
    "wrangler": "^4.81.0"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
